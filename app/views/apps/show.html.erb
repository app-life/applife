<div class="container">
 <div class="row">
      <% @apps.each do |app| %>
  <div class="col-md-6">
      <p><%= app.title%></p>
      <p><%#= app.body%></p>
      <p><%= markdown(app.body).html_safe %></p>

  </div>
    <% end %>

 </div>
</div>